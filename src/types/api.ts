export type ErrorI = {
  error: string;
  status: number;
};

export type SuccessI = {
  message: string;
  status: number;
};

export type ResponseImageI = {
  image: string;
  status: number;
};

export type DeleteImageI = {
  imgSrc: string;
};
